syntax = "proto2";

package harmonyServer;

message StartOps {
  required string scenarioId = 1;
  required Inputs inputs = 2;
  required Outputs outputs = 3;

  message Inputs {
    required string ANGFile = 1;
  }

  message Outputs {
    required string Sample = 1;
  }
}